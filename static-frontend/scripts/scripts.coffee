Portal = {}

Portal.apps =
  paginador: ->
    # YUI().use 'paginator', 'node', (Y) ->
    #   pageUI = Y.one '.chamadas'
    #   pg = new Y.Paginator {
    #     totalItems: 5,
    #     itemsPerPage: 1
    #   }
    #   return
    return

do ->
  Portal.apps.paginador()
  return
